<div class="card">
  <div class="card-header">
    <%= course.title %>
  </div>
  <div class="card-body">
    <%= course.description %>
  </div>
  <div class="card-footer">
    <%= course.created_at %>
    <%= course.user %>
  </div>
</div>
